<mvc:View
    controllerName="ns.travels.controller.InvoiceList"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc"
>
    <List
        id="invoice-list"
        headerText="{i18n>invoiceListTitle}"
        class="sapUiResponsiveMargin"
        width="auto"
        items="{
            path: 'invoice>/Invoices',
            sorter: {
                path: 'ShipperName',
                group: true
            }
        }"
    >
        <headerToolbar>
            <Toolbar id="invoice-list-toolbar">
                <Title
                    id="invoice-list-title"
                    text="{i18n>invoiceListTitle}"
                />
                <ToolbarSpacer id="invoice-list-spacer" />
                <SearchField
                    id="invoice-list-search-field"
                    width="50%"
                    search=".onFilterInvoices"
                />
            </Toolbar>
        </headerToolbar>
        <items>
            <ObjectListItem
                id="item-invoice"
                core:require="{
                    Currency: 'sap/ui/model/type/Currency'
                }"
                title="{invoice>Quantity} x {invoice>ProductName}"
                number="{
                    parts: [
                        'invoice>ExtendedPrice',
                        'view>/currency'
                    ],
                    type: 'Currency',
                    formatOptions: {
                        showMeasure: false
                    }
                }"
                numberUnit="{view>/currency}"
                numberState="{= ${invoice>ExtendedPrice} > 50 ? 'Error' : 'Success' }"
                type="Navigation"
                press=".onPress"
            >
                <firstStatus>
                    <ObjectStatus
                        id="object-status"
                        core:require="{
                            Formatter: 'ns/travels/model/formatter'
                        }"
                        text="{
                            path: 'invoice>Status',
                            formatter: 'Formatter.statusText.bind($controller)'
                        }"
                    />
                </firstStatus>
            </ObjectListItem>
        </items>
    </List>
</mvc:View>
